import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  userText: {
    marginTop: 10,
    fontSize: 18,
    fontWeight: 'bold',
    textAlign: 'center',
  },
  textArea: {
    marginVertical: 20,
    width: '90%',
    borderColor: 'gray',
    borderWidth: 1,
    padding: 8,
    fontSize: 16,
    textAlignVertical: 'top',
  },
});
