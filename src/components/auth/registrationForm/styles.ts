import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: '#777777',
    borderRadius: 1,
    height: 50,
    width: '90%',
    marginTop: 10,
    paddingLeft: 15,
  },
});
